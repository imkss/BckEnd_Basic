<%- include("headers") -%>
    <section class="vh-100 gradient-custom">
      <div class="container py-5 h-100">
        <div class="row d-flex justify-content-center align-items-center h-100">
          <h1>Keep Your Short Notes Here<br /><b>🖊</b></h1>
          <div class="col col-xl-10">
            <div class="card">
              <div class="card-body p-5">
                <h2><%= listTitle %></h2>

                <ul class="list-group card-body p-sm-2">
                  <% for(var i=0; i < newListItems.length; i++) { %>

                  <li class="list-group-item">
                    <input class="tick" type="checkbox" />

                    <h6><b>🎯 </b><%= newListItems[i] %></h6>
                  </li>
                  <% } %>
                </ul>
                <form
                  action="/"
                  method="post"
                  class="list-group card-body p-sm-2 d-flex justify-content-center align-items-center h-100"
                >
                  <input
                    type="text"
                    name="newItem"
                    autocomplete="off"
                    placeholder="New Item"
                  /><br />
                  <button
                    class="btn btn-info ms-2 button"
                    type="submit"
                    name="list"
                    // value=<%= listTitle %> NEVER SAVE THIS PAGE :) // 
                    value=<%= listTitle %>

                  >
                    ➕
                  </button>
                </form>
              </div>
            </div>
          </div>
        </div>
        <%- include("footers") -%>